import "package:flutter/material.dart";

class SearchCoworking extends StatefulWidget {
  const SearchCoworking({super.key});

  @override
  State<SearchCoworking> createState() => _SearchCoworking();
}

class _SearchCoworking extends State<SearchCoworking> {
  @override

  Widget build(BuildContext context) {
    return Container(
      color: const Color(0xFF0A0F2A),
    );
  }
}
